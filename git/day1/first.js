const name = "rohit"
const age = 22
let balance = 420

function updatebalance(value){
    balance += value;
}

const a = 5;
const b = 8;
const c = b+a;

// while(true){
    // console.log(20)
// }

// simply git is version control system which is used to track all the changes done in a code and also
// like a company website is down then they will undo all the changes in the website code before done 
// when it get down

// in simple term it is used to maintain the history of the code